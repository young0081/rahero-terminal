# 拉海洛终端 · 项目说明（CLAUDE.md）

《鸣潮》星炬学院「拉海洛终端」跨平台复刻。开源同人，非官方。

## 技术栈

- **框架**：Flutter 3.44 / Dart 3.12（stable）
- **状态管理**：Riverpod（flutter_riverpod）
- **路由**：go_router
- **本地存储**：Hive（仅存简单类型，不用代码生成）
- **网络下载**：dio
- **视频播放**：video_player（移动端）+ media_kit（桌面端）
- **目录/缓存**：path_provider
- **校验**：crypto（sha256）
- **目标平台**：Windows / Linux / Android

## 核心设计

1. **资源运行时下载**：受版权的图标/视频不入库，首次运行从库街区 CDN 下载、本地缓存、离线可用。清单驱动（`assets/manifest/asset_manifest.json`），增量下载 + sha256 校验 + 原子写入 + 离线降级（占位图）。
2. **占位优先**：飞讯剧本、势力简介先用占位内容，结构定好，后续替换真实文本。
3. **占位图不崩**：资源未就绪时用 `PlaceholderIcon`（CustomPainter 代码绘制）兜底，绝不空白或崩溃。
4. **响应式 + 双输入**：桌面侧边导航、移动端底部导航，按断点切换布局。

## 目录结构

```
拉海洛终端/
├── lib/
│   ├── main.dart                      # 入口：初始化存储/媒体内核 → 路由
│   ├── router/app_router.dart         # 路由：开机 → 资源下载 → 主壳
│   ├── core/
│   │   ├── theme/app_theme.dart       # 配色 token（青柠绿/蓝/银/黑）+ 主题
│   │   ├── storage/app_storage.dart   # Hive 封装（settings/progress/assets）
│   │   ├── responsive/responsive.dart # 断点与设备类型
│   │   ├── widgets/
│   │   │   ├── dynamic_icon_view.dart # 统一动态图标（视频/图/占位降级）
│   │   │   └── placeholder_icon.dart  # 代码绘制占位图
│   │   └── effects/scanline_overlay.dart # 扫描线/暗角叠加 + 终端面板
│   ├── data/asset_manifest.dart       # 资源清单数据模型
│   └── features/
│       ├── boot/boot_screen.dart      # 开机序列
│       ├── provisioning/              # 资源下载（service + providers + screen）
│       ├── shell/                     # 主壳 + 设置页
│       ├── feixun/                    # 飞讯（data + screen）
│       ├── archive/                   # 势力档案（data + screen）
│       ├── codex/                     # 图鉴（资料库）
│       ├── profile/                   # 个人信息（我的）
│       ├── achievements/              # 成就系统
│       ├── search/                    # 全局搜索
│       ├── onboarding/                # 新手引导
│       └── theming/                   # 主题管理
├── assets/
│   ├── manifest/asset_manifest.json   # 资源清单（上线前回填 baseUrl/checksum）
│   ├── scripts/                       # 飞讯剧本 + 势力简介（占位内容）
│   └── placeholders/                  # 占位说明（占位图由代码绘制）
├── scripts/fetch_windows_libs.sh      # 国内镜像预置 media_kit Windows 视频库
├── windows/ linux/ android/           # 三平台原生工程
└── test/                              # 测试文件
```

## 常用命令

```bash
# 国内镜像（每次新终端先设置）
export PUB_HOSTED_URL=https://pub.flutter-io.cn
export FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn

flutter pub get            # 装依赖
dart analyze               # 静态分析（应为 No issues found）
flutter test               # 运行测试
flutter run -d windows     # 跑桌面 Windows
flutter run -d linux       # 跑桌面 Linux
flutter run -d <设备号>     # 跑 Android

flutter build windows      # 打包 Windows（首次需 scripts/fetch_windows_libs.sh）
flutter build linux        # 打包 Linux
flutter build apk          # 打包 Android
```

## 已知约束

- 中文目录名 `拉海洛终端` 不能作 Dart 包名，包名固定为 `rahero_terminal`。
- Windows 构建依赖 media_kit 的 ANGLE/libmpv（首次从 GitHub 下载，国内用 `scripts/fetch_windows_libs.sh` 预置）。
- `flutter doctor` 中 Chrome（web）与 Google 网络检查项可忽略，本项目无 web 目标、全程走国内镜像。
- 资源 `baseUrl` 上线前为占位地址，此时全程用占位图，功能不受影响。
- Linux 桌面构建需在 Linux 机器上执行（clang/cmake/ninja/GTK 工具链）。

## 已完成功能

### 核心功能
- [x] 环境搭建（Flutter SDK、Android 工具链、镜像）
- [x] 项目骨架 + 依赖 + 资源目录 + 清单
- [x] 核心层（主题、存储、响应式、动态图标、占位图、效果层）
- [x] 资源下载模块（增量下载、校验、降级、缓存管理）
- [x] 开机页 / 资源下载页 / 主壳 / 飞讯 / 势力档案 / 设置

### 图鉴系统（资料库）
- [x] 对接库街区 wiki：共鸣者/武器/声骸 实时列表+详情，结构化表格渲染
- [x] 图鉴图片：立绘画廊+正文内嵌图+表格图标，按 URL 去重惰性下载（并发限 5）、离线缓存
- [x] 图鉴定时自动更新：启动+周期（默认每天凌晨4点，可改每天/每小时/每分钟/仅启动）指纹比对增量同步
- [x] 角色语音：解析 audio-component，按分类列出+点击播放(.wav 缓存)+台词文本
- [x] 详情演示图排版：铺满宽度显示+点击全屏缩放查看

### 个人信息系统
- [x] 个人信息板块「我的」：名片+使用统计+收藏夹（纯本地，无需联网）
- [x] 势力档案自动抓取：运行时从库街区"名词注释-背景"分类筛选势力词条，定时增量更新

### 新增功能（v1.1）
- [x] **成就系统**：
  - 15+ 个成就，涵盖飞讯、图鉴、收藏、探索、系统等分类
  - 实时进度追踪，达成目标自动解锁
  - 成就解锁弹窗提示
  - 成就完成度统计与徽章展示
  - 成就触发器集成到各功能模块

- [x] **全局搜索**：
  - 统一搜索入口（顶部搜索图标）
  - 支持搜索共鸣者、武器、声骸、飞讯消息
  - 实时搜索结果展示，带分类标签
  - 防抖优化，300ms 延迟搜索

- [x] **新手引导**：
  - 7 步图文引导，介绍主要功能
  - 首次启动自动显示
  - 设置页面可重新查看
  - 支持跳过和分页导航

- [x] **主题切换**：
  - 5 套预设主题（冷调银灰、星炬绿、深空红、紫色科幻、琥珀暖调）
  - 动态颜色切换，实时生效
  - 主题预览与描述
  - 本地存储主题偏好

### 新增功能（v1.2 - 2026-06-04）
- [x] **好感度系统**：
  - 与角色互动（发送消息）增加好感度
  - 6个好感度等级（陌生→熟识→友好→信任→亲密→挚友）
  - 好感度进度条与等级显示
  - 升级时弹窗提示
  - 联系人列表显示好感度等级标签
  - 关联成就系统（信任、挚友成就）

- [x] **每日签到系统**：
  - 每日签到获得数据点数（虚拟货币）
  - 基础奖励 10 点，连续签到额外奖励
  - 连续3天+5点，连续7天+20点
  - 签到记录与统计（连续天数、累计次数）
  - 精美签到界面与奖励动画
  - 关联成就系统（连续签到成就）

- [x] **UGC剧本系统**：
  - 创作自定义飞讯对话剧本
  - 可视化编辑器（添加/编辑/删除/排序对话）
  - 剧本元数据（标题、作者、创建时间）
  - 剧本列表管理界面
  - 导出为JSON文件分享
  - 导入他人创作的剧本
  - 关联成就系统（创作成就）

### 构建与测试
- [x] Windows 构建通过 + 测试通过（29）+ 静态分析零问题

## 规划中功能（按优先级）

### P1 - 高价值功能增强
- [ ] **飞讯系统深化**
  - [ ] 角色好感度机制：记录互动次数，解锁隐藏剧情
  - [ ] 语音消息：集成角色配音片段
  - [ ] 群聊功能：多角色群组对话
  
- [ ] **图鉴系统扩展**
  - [ ] 对比模式：并排对比两个角色/武器的属性
  - [ ] 养成模拟器：材料计算器、培养进度追踪
  - [ ] 关系网络图：可视化角色间的人物关系

- [ ] **势力档案深化**
  - [ ] 势力成员清单：点开势力后显示所属角色
  - [ ] 势力领地地图：交互式地图标注控制区域

- [ ] **终端拟真化**
  - [ ] 启动音效、按键反馈音
  - [ ] 环境音（可调音量）
  - [ ] 全息投影效果
  - [ ] 数据流动画

### P2 - 差异化与优化
- [ ] **每日签到与任务系统**
- [ ] **数据统计可视化**（使用时长曲线、热力图）
- [ ] **名片分享**（生成精美图片）
- [ ] **剧本编辑器**（用户创作飞讯对话）
- [ ] **多语言支持**（英文/繁体中文/日文）
- [ ] **无障碍优化**（字体调节、色盲模式、屏幕阅读器）

### P3 - 长期目标
- [ ] Android 真机/模拟器运行验证
- [ ] Linux 桌面构建验证
- [ ] 上线：素材上传库街区、回填 baseUrl/checksum
- [ ] 内容补充：真实飞讯剧情

## 技术亮点

1. **运行时资源下载系统**：清单驱动、增量下载、sha256 校验、原子写入、离线降级
2. **成就系统**：事件驱动触发器、进度持久化、解锁动画
3. **全局搜索**：跨模块统一搜索、防抖优化、结果分类展示
4. **主题系统**：动态配色、多套预设、实时切换
5. **新手引导**：分步引导、首次自动显示、可重复查看

## 最近更新（v1.2 - 2026-06-04）

- ✅ 新增好感度系统（6个等级，互动增长，升级提示）
- ✅ 新增每日签到系统（虚拟货币、连续奖励、签到记录）
- ✅ 新增UGC剧本系统（创作/编辑/分享自定义飞讯对话）
- ✅ 飞讯联系人列表显示好感度等级标签
- ✅ 飞讯发送消息自动增加好感度
- ✅ 设置页面添加签到和UGC入口
- ✅ 成就系统新增8个成就（好感度、签到、创作类）
- ✅ 所有新功能与成就系统联动
