## 1. 消息状态模型与持久化兼容

- [x] 1.1 在 `FeixunMessage` 增加可选 `status` 字段（sending/sent/failed），缺省视为 sent
- [x] 1.2 `fromJson`/`toJson` 支持 status，缺字段时容错回退为 sent
- [x] 1.3 `ChatLog` 仅持久化终态消息，重启不恢复 sending 幽灵消息
- [x] 1.4 新增序列化往返测试：含 status 与"旧数据无 status 字段"两种用例

## 2. 传输抽象与模拟实现

- [x] 2.1 定义 `MessageTransport` 抽象接口（发送、接收回调）
- [x] 2.2 实现 `MockMessageTransport`（可配延迟、可配失败率，默认成功且短延迟）
- [x] 2.3 `ChatLog` 发送经由 transport 驱动状态流转（sending → sent/failed）

## 3. 输入栏精修

- [x] 3.1 精修 `_InputBar` 样式（圆角、内边距、占位提示、聚焦态高亮）贴合终端美学
- [x] 3.2 多行扩展（上限内扩展、超限内部滚动），不挤压消息流
- [x] 3.3 空白消息拒绝发送的校验与发送键/回车一致行为

## 4. 气泡视觉精修

- [x] 4.1 统一气泡最大宽度、留白、配色与时间戳排布
- [x] 4.2 校验长文本自动换行不溢出、双方气泡颜色/对齐区分

## 5. 表情完整呈现修复

- [x] 5.1 表情栏格子用固定基线尺寸 + BoxFit.contain，等比不裁剪、尺寸一致
- [x] 5.2 气泡内表情消息完整显示，不被边界裁剪或拉伸
- [x] 5.3 贴图未就绪时 unicode 字形居中完整兜底（FittedBox 等比缩放、不截断）

## 6. 气泡出现动画

- [x] 6.1 新消息气泡进入过渡（淡入 + 轻微位移/缩放，≤250ms），用内置 TweenAnimationBuilder
- [x] 6.2 列表项用稳定 key，避免滚动/重建时误重放动画
- [x] 6.3 发送后自动滚动到底部与动画时序协调

## 7. 收发加载态与加载动画

- [x] 7.1 发送中气泡显示加载指示（转圈/半透明）
- [x] 7.2 失败气泡显示失败标记 + 重试入口，点击重试重发
- [x] 7.3 对方接收中显示打字指示加载气泡（三点跳动动画）
- [x] 7.4 消息到达后移除打字指示，以出现过渡动画替换为真实气泡

## 8. 集成与验证

- [x] 8.1 dart analyze 零问题
- [x] 8.2 flutter test 全绿（含新增状态/序列化测试，共 7 条）
- [x] 8.3 debug 构建启动冒烟通过
- [x] 8.4 重启恢复逻辑：旧数据无 status 字段视为 sent，仅终态持久化（单测覆盖）
- [ ] 8.5 三端表现一致性确认（仅验证 Windows；Linux/Android 待对应环境构建确认，代码均用跨平台 Flutter 原语）
- [x] 8.6 打包发布版 exe 并更新到桌面
