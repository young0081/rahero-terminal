## 1. 环境准备与工具链（我执行，用户零操作）

- [x] 1.1 下载安装 Flutter SDK 稳定版，配置 PATH，确认 `flutter --version` / `dart --version` 可用
- [x] 1.2 运行 `flutter config` 关联已装 Android SDK 与 Visual Studio 桌面构建链，启用 windows、android 平台支持
- [x] 1.3 运行 `flutter doctor` 并修复所有问题，直至 Windows 桌面与 Android 工具链全部通过
- [x] 1.4 接受 Android 许可（`flutter doctor --android-licenses`），确认可识别模拟器 / 真机
- [ ] 1.5 在本机 Ubuntu 安装 Flutter SDK 与 Linux 桌面构建依赖（clang / cmake / ninja / GTK 开发库等），`flutter doctor` 通过 Linux toolchain，供产出 Linux 二进制（待 Linux 机器执行）

## 2. 项目骨架与依赖

- [x] 2.1 用 `flutter create` 生成支持 windows / linux / android 的工程骨架（包名 rahero_terminal，中文目录名不可作包名）
- [x] 2.2 添加依赖：flutter_riverpod、hive + hive_flutter、media_kit（桌面视频）、video_player（Android 视频）、go_router、path_provider、dio、crypto，并执行 `flutter pub get`
- [x] 2.3 建立 feature 分层目录结构（core / features/{boot,provisioning,shell,feixun,archive} / data / router）
- [x] 2.4 在 pubspec.yaml 声明 assets（manifest / placeholders / scripts）；动态图标不入仓库（运行时下载）
- [x] 2.5 编写素材清单初稿 `assets/manifest/asset_manifest.json`（9 个势力/组织 + 开机动画；id / 来源 / 缓存路径 / 类型 / 校验），占位图由代码绘制
- [x] 2.6 配置 .gitignore（排除 build/、.dart_tool/、本地密钥、素材源目录、缓存等），编写项目 CLAUDE.md 与 README.md
- [x] 2.7 跑通空壳应用：Windows 构建出 exe、Android 构建出 apk、widget 测试通过（骨架验证关卡）

## 3. 核心层（core）

- [x] 3.1 实现主题令牌系统 AppTheme：配色（黑底 / #b4f064 青柠绿 / #6078d8 蓝 / #f0f0f0 白银 / 辉光）、字体、边框几何、间距、辉光强度
- [ ] 3.2 打包并接入中文字体（可商用免费字体子集）与等宽科技英文字体，验证三端字形一致（待补充字体文件）
- [x] 3.3 实现媒体抽象 DynamicIconView：从本地缓存目录按素材清单解析路径，按类型分发 MP4(video_player) / GIF / 图片（用 `Image.file`），支持循环播放与首帧静帧
- [x] 3.4 为 DynamicIconView 实现加载失败降级链（素材未下载/文件缺失/解码失败 → 代码绘制占位图）与控制器释放逻辑
- [x] 3.5 封装 Hive 持久化层（初始化、盒子管理、读写接口）用于会话进度 / 已读 / 用户设置 / 缓存状态
- [x] 3.6 实现氛围特效层（扫描线 / 暗角 / 移动高光带），用 CustomPainter，叠加层 IgnorePointer 不拦截交互
- [ ] 3.7 实现特效降级机制（帧率监测 / 性能模式开关，低性能自动降级或关闭）（已留 intensity/animate 开关，自动监测待补充）
- [x] 3.8 实现响应式基础（断点 + Responsive 助手）与桌面/移动双导航（桌面 NavigationRail / 移动 NavigationBar，触控 ≥48dp）

## 4. 素材下载与缓存模块（asset-provisioning）

- [x] 4.1 实现素材清单解析（读取 `asset_manifest.json`，得到每项 id / URL / 缓存相对路径 / 类型 / 校验信息）
- [x] 4.2 用 path_provider 实现跨平台缓存目录解析（桌面 application support、Android 应用专属目录），统一子目录与相对路径逻辑
- [x] 4.3 实现增量下载器（dio）：比对清单与缓存，仅下载缺失 / 校验失败项；逐项下载到 .part 临时文件、sha256 校验通过再原子改名落盘，失败删除重下
- [x] 4.4 实现下载进度 UI（features/provisioning）：当前项 / 总体百分比 / 状态，已缓存完整则跳过直接进入
- [x] 4.5 实现下载失败与离线降级：单项失败用占位图、不崩溃不阻断进入；地址未配置时全程占位形态进入；可手动进入
- [x] 4.6 实现设置内"重新下载素材"入口与缓存管理（清理缓存，清理后下次启动重新下载）
- [ ] 4.7 单独验证素材模块各路径（待真实下载地址配置后端到端验证；逻辑已就位、离线降级已验证）

## 5. 开机序列模块（boot-sequence）

- [~] 5.1 接入"深空联合（带文字）"动态图标，实现全屏黑底开机动画播放（清单已含 boot 项，DynamicIconView 可承载；当前用文字开机序列，素材就绪后增强）
- [x] 5.2 实现冷启动播放完整序列 / 资源缺失（含未下载）降级到文字开机序列的分支
- [x] 5.3 实现终端自检打字机文案序列（逐行输出）
- [ ] 5.4 实现点击 / 按键跳过：立即结束动画与文案进入主界面
- [x] 5.5 实现启动完成到下一步的转场（含扫描线叠加）
- [ ] 5.6 实现热启动跳过完整动画、恢复上次界面状态
- [ ] 5.7 单独验证开机模块四条路径（冷启动/降级已通过构建验证；跳过/热启动待补充）

## 6. 终端外壳模块（terminal-shell）

- [x] 6.1 实现主界面外壳布局：导航区 + 主内容容器（桌面侧栏 / 移动顶栏+底栏）
- [ ] 6.2 实现顶部状态栏：终端标识、实时系统时钟、在线状态指示灯
- [x] 6.3 实现模块导航与选中高亮态，可在飞讯 / 势力档案 / 设置间切换
- [ ] 6.4 实现模块切换的科技转场动画（淡入 / 滑动 / 扫描线）
- [x] 6.5 默认进入飞讯模块，主内容区不空白
- [x] 6.6 应用主题系统与氛围特效层（ScanlineOverlay），确保全局视觉一致
- [x] 6.7 实现外壳层响应式：桌面侧栏并排 / 移动底栏，按断点切换
- [ ] 6.8 单独验证外壳：状态栏实时更新、转场动画（基础渲染/导航/响应式已通过构建验证）

## 7. 势力资料库模块（faction-archive）

- [x] 7.1 定义势力条目只读数据模型并填充资源清单支持的 9 个势力/组织占位条目（星炬学院、深空联合、残星会、瑝珑、稷庭、七丘、黑海岸、拉古那、先行公约）
- [x] 7.2 将缓存动态图标按条目接入，缺素材条目用代码绘制占位图标
- [x] 7.3 实现图鉴网格（响应式列数，每项动态图标 + 名称）
- [x] 7.4 实现条目详情视图：动态图标 + 名称 + 简介（占位文案先行），离开释放资源
- [ ] 7.5 实现按名称实时检索与按类别筛选
- [ ] 7.6 单独验证资料库各路径（网格/占位/详情已通过构建验证；检索分类待补充）

## 8. 飞讯模块（feixun-messaging）

- [x] 8.1 定义飞讯剧情脚本数据模型（联系人、消息节点）并编写占位脚本 JSON 随包（实机截图还原基准待补充）
- [x] 8.2 还原联系人列表：头像（复用动态图标）、名称、最近消息摘要、置顶标记
- [x] 8.3 还原对话会话视图：气泡流、双方消息样式与对齐区分、按顺序排列
- [ ] 8.4 实现消息流逐条推进呈现（打字 / 淡入动画，按脚本节奏）
- [ ] 8.5 实现回复选项分支：底部可点击选项，按选择推进后续消息
- [ ] 8.6 实现新消息推送角标：不在飞讯模块时导航入口显示未读提示
- [ ] 8.7 用 Hive 持久化会话进度与已读状态，重启恢复（存储层已就位，接线待补充）
- [ ] 8.8 单独验证飞讯各路径（列表/会话区分已通过构建验证；逐条推进/分支/角标/恢复待补充）

## 9. 整体联调与三端自验证

- [x] 9.1 端到端流程串通：素材下载 → 开机序列 → 外壳 → 资料库 / 飞讯路由无断点
- [x] 9.2 运行 `dart analyze`，修复至零警告 / 零错误，清理调试日志（No issues found）
- [x] 9.3 `flutter build windows` 构建通过，产出 rahero_terminal.exe
- [x] 9.4 `flutter build apk` 构建通过，产出 app-debug.apk（真机/模拟器实跑验证待补充）
- [ ] 9.5 在本机 Ubuntu 执行 `flutter build linux` 构建通过并实跑 Linux 产物验证（待 Linux 机器执行）
- [ ] 9.6 性能与降级验证：特效降级、媒体按需加载内存可控、双输入两端可操作、离线占位降级
- [x] 9.7 完善项目 CLAUDE.md、README.md（大白话启动说明 + 可复制命令 + 首次需联网说明）、素材性质声明
